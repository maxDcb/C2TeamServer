{
  "name": "shell",
  "description": "Start or use the persistent interactive shell.",
  "command_template": "shell {command:raw?}",
  "parameters": {
    "type": "object",
    "properties": {
      "beacon_hash": {
        "type": "string",
        "description": "Full beacon hash identifying the session that should execute the command."
      },
      "listener_hash": {
        "type": "string",
        "description": "Full listener hash for the target beacon session."
      },
      "command": {
        "type": "string",
        "description": "Optional shell command. Leave empty to start the shell; use exit to stop it.",
        "default": ""
      }
    },
    "required": [
      "beacon_hash",
      "listener_hash"
    ],
    "additionalProperties": false
  }
}
