{
  "name": "mkDir",
  "description": "Create a directory on the beacon host.",
  "command_template": "mkDir {path:q}",
  "parameters": {
    "type": "object",
    "properties": {
      "beacon_hash": {
        "type": "string",
        "description": "Full beacon hash identifying the session that should execute the command."
      },
      "listener_hash": {
        "type": "string",
        "description": "Full listener hash for the target beacon session."
      },
      "path": {
        "type": "string",
        "description": "Directory path to create."
      }
    },
    "required": [
      "beacon_hash",
      "listener_hash",
      "path"
    ],
    "additionalProperties": false
  }
}
