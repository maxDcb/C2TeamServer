{
  "name": "tree",
  "description": "Recursively list a directory tree on a beacon host.",
  "command_template": "tree {path:q}",
  "parameters": {
    "type": "object",
    "properties": {
      "beacon_hash": {
        "type": "string",
        "description": "Full beacon hash identifying the session that should execute the command."
      },
      "listener_hash": {
        "type": "string",
        "description": "Full listener hash for the target beacon session."
      },
      "path": {
        "type": "string",
        "description": "Directory root to inspect."
      }
    },
    "required": [
      "beacon_hash",
      "listener_hash",
      "path"
    ],
    "additionalProperties": false
  }
}
