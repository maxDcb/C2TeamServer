{
  "name": "screenShot",
  "description": "Capture a screenshot from the beacon host.",
  "command_template": "screenShot",
  "parameters": {
    "type": "object",
    "properties": {
      "beacon_hash": {
        "type": "string",
        "description": "Full beacon hash identifying the session that should execute the command."
      },
      "listener_hash": {
        "type": "string",
        "description": "Full listener hash for the target beacon session."
      }
    },
    "required": [
      "beacon_hash",
      "listener_hash"
    ],
    "additionalProperties": false
  }
}
