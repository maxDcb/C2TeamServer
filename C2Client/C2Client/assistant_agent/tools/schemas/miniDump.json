{
  "name": "miniDump",
  "description": "Dump LSASS to an XORed file or decrypt an XORed dump locally.",
  "command_template": "miniDump {action} {path:q}",
  "parameters": {
    "type": "object",
    "properties": {
      "beacon_hash": {
        "type": "string",
        "description": "Full beacon hash identifying the session that should execute the command."
      },
      "listener_hash": {
        "type": "string",
        "description": "Full listener hash for the target beacon session."
      },
      "action": {
        "type": "string",
        "description": "MiniDump action.",
        "enum": [
          "dump",
          "decrypt"
        ]
      },
      "path": {
        "type": "string",
        "description": "Output file for dump, or input XORed dump path for decrypt."
      }
    },
    "required": [
      "beacon_hash",
      "listener_hash",
      "action",
      "path"
    ],
    "additionalProperties": false
  }
}
