{
  "name": "enumerateRdpSessions",
  "description": "Enumerate local or remote RDP sessions.",
  "command_template": "enumerateRdpSessions [-s {server:q}]",
  "parameters": {
    "type": "object",
    "properties": {
      "beacon_hash": {
        "type": "string",
        "description": "Full beacon hash identifying the session that should execute the command."
      },
      "listener_hash": {
        "type": "string",
        "description": "Full listener hash for the target beacon session."
      },
      "server": {
        "type": "string",
        "description": "Optional target host name or IP. Omit for local enumeration.",
        "default": ""
      }
    },
    "required": [
      "beacon_hash",
      "listener_hash"
    ],
    "additionalProperties": false
  }
}
