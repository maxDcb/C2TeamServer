{
  "name": "ls",
  "description": "List a directory on a beacon host.",
  "command_template": "ls {path:q}",
  "parameters": {
    "type": "object",
    "properties": {
      "beacon_hash": {
        "type": "string",
        "description": "Full beacon hash identifying the session that should execute the command."
      },
      "listener_hash": {
        "type": "string",
        "description": "Full listener hash for the target beacon session."
      },
      "path": {
        "type": "string",
        "description": "Directory path to list."
      }
    },
    "required": [
      "beacon_hash",
      "listener_hash",
      "path"
    ],
    "additionalProperties": false
  }
}
